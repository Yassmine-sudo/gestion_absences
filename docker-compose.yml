version: "3.8"

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    depends_on:
      - selenium

  selenium:
    image: selenium/standalone-chrome
    ports:
      - "4444:4444"
